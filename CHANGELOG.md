# 更新日志 (Changelog)

本项目的所有重要变更将记录在此文件中。

## [Unreleased] - 2026-01-18

### 🚀 新增功能 (Features)
- **音频数据工程 (Audio Data Engineering)**
  - 实现全量圣经音频下载脚本 `scripts/download_full_audio.py`。
  - 完成全部 66 卷书、1189 章音频文件的下载 (100% 覆盖)。
  - 生成经过验证的音频 URL 列表 `data/bible_audio_urls.txt`。
  - 实现音频格式批量转换工具 `scripts/convert_audio_opus.py` (MP3 -> Opus 24k)。
  - 完成所有音频文件的格式转换 (1189 files)，显著减小体积。
  - 添加清理脚本 `scripts/cleanup_bad_folders.sh` 用于移除错误的下载目录。

- **核心阅读体验 (Core Reading)**
  - 实现基于 Clean Architecture 的数据层、领域层和表现层。
  - 完成 "书卷列表" (HomePage) 和 "经文阅读" (ChapterPage) 界面。
  - 集成 `sqflite` 实现本地离线阅读。

- **数据采集 (Data Scraping)**
  - 实现圣经文本抓取脚本 `scripts/scrape_bible.py` (Source: bible.com)。
  - 构建 SQLite 数据库构建脚本 `scripts/build_db.py`。
  - 完成和合本 (CUNPSS-神) 文本数据的本地化存储 (`assets/bible.db`)。

### 🐛 修复与优化 (Fixes & Improvements)
- **音频下载 URL 修复**:
  - 修复带数字书卷 (如 1 Samuel) URL 缺少空格导致的 404 错误。
  - 修正 `Psalms` 为 `Psalm` 以匹配源站命名。
  - 修正 `Thessalonians` 为缩写 `Thess` 以匹配源站命名。
  - 修正 `Deuteronomy` 拼写问题。
- **构建系统**:
  - 创建 `dev.sh` 统一启动脚本。
  - 配置 `cocoapods` 解决 iOS 构建依赖问题。

## [0.1.0] - Projekt Init

### 🎉 初始化 (Initialization)
-以此 FlutterCleanArchitecture 模板初始化项目结构。
- 配置 `pubspec.yaml` 基础依赖 (`flutter_bloc`, `get_it`, `sqlite`, etc.)。
- 确立包名为 `com.yishulun.enyan`。
- 应用重命名为 "恩言大字圣经"。
